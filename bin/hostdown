#!/usr/bin/env bash

echo "- Setting hostnames" &&
sudo npm run hostile -- remove redis.poutineer.local &&
sudo npm run hostile -- remove postgres.poutineer.local &&
sudo npm run hostile -- remove resources.poutineer.local
